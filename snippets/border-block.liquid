{%- doc -%}
  Renders the 'Border' section block.

  @param {object} [shopify_attributes] - The block's shopify attributes.
{%- enddoc -%}

<div class="border-block section-blocks__border" {{ shopify_attributes }}></div>
